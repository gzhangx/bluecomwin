<Window x:Class="WpfBlueTooth.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfBlueTooth"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="txtInfo" MaxLines="20" ></TextBox>
        <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal">
            <Button  x:Name="test" Click="Test_Click" Margin="2">Pair</Button>
            <Button  x:Name="btnSend" Click="BtnSend_Click" Margin="2">Send</Button>
        </StackPanel>
        
    </Grid>
</Window>
